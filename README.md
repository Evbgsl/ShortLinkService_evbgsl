# ShortLinkService_evbgsl

Сервис сокращения ссылок на Java с поддержкой лимита переходов и времени жизни (TTL).
Проект реализован для учебного задания, включает консольный интерфейс и автоматическое тестирование.


## 📦 Структура проекта
```bash
src/
├─ main/
│   ├─ java/com/evbgsl/shortlinkservice/
│   │   ├─ model/          # классы ShortLink, User
│   │   ├─ service/        # LinkService, CleanupService, NotificationService, UserService
│   │   ├─ util/           # JsonStorage, AppConfig, LinkGenerator
│   │   └─ Main.java       # точка входа приложения
│   └─ resources/
│       └─ app.properties  # конфигурация TTL и другие параметры
└─ test/
├─ java/com/evbgsl/shortlinkservice/model/
│   ├─ ShortLinkTest.java
│   └─ ShortLinkLimitTest.java
└─ java/com/evbgsl/shortlinkservice/service/
└─ LinkServiceTest.java
```
---
## ⚙️ Установка и запуск

#### Клонируйте репозиторий:
```yaml
git clone https://github.com/Evbgsl/ShortLinkService_evbgsl.git
cd ShortLinkService_evbgsl
```
#### Соберите проект через Maven:
```bash
mvn compile
```
#### Запустите консольное приложение:
```bash
mvn exec:java -Dexec.mainClass="com.evbgsl.shortlinkservice.Main"

```
---
## 📝 Конфигурация
Файл `src/main/resources/app.properties` содержит ключевые параметры:
```declarative
ttl.minutes=180   # время жизни ссылки (TTL) по умолчанию в минутах
max.visits=10     # количество переходов по ссылке по умолчанию  
```
- TTL подтягивается из конфигурации и используется как максимум при создании ссылок. 
- Лимит переходов подтягивается из конфигурации и используется как максимум при создании ссылок.
- Оба параметра могут быть заданы пользователем при создании ссылки (не более 300 мин, не более 10 переходов).
---
## 🖥️ Консольное меню
1. Создать короткую ссылку
2. Показать мои ссылки
3. Перейти по короткой ссылке
4. Очистить просроченные ссылки (ручная очистка, автоочистка каждые 60 секунд)
5. Выход из программы

Пример создания ссылки:
```text
Сделайте Ваш выбор (введите номер пункта меню): 1
Введите оригинальный URL: https://mediametrics.ru/rating/ru/hour.html
Введите лимит переходов по ссылке (целое число от 1 до 10): 5
Введите TTL в минутах (не более 300 минут, Enter для значения по умолчанию - 180 минут): 150
Короткая ссылка создана! Код: YCnNW3 | Лимит переходов: 5 | TTL: 150 мин
```
---

## 🔗 Пример вывода списка ссылок
```text
Ваши ссылки:
[OK] Код: sVzZ4k | URL: https://mephi.ru/ | Совершенные переходы: 0/5 | Срок жизни (осталось): 2h 57m | Создана: 09.11.2025 20:10 | Истекает: 09.11.2025 23:10
[OK] Код: YCnNW3 | URL: https://mediametrics.ru/rating/ru/hour.html | Совершенные переходы: 0/5 | Срок жизни (осталось): 2h 28m | Создана: 09.11.2025 20:11 | Истекает: 09
.11.2025 22:41
```
- [OK] — ссылка активна
- [TTL истёк] — срок действия ссылки истёк
- [Лимит] — достигнут лимит переходов

---

## 🧪 Тестирование
Unit-тесты находятся в src/test/java.

#### Основные тесты:
- ShortLinkLimitTest — проверка лимита переходов
- ShortLinkTest — проверка TTL и времени жизни
- LinkServiceTest — интеграционные сценарии: создание ссылок, проверка TTL, уведомлений и лимитов

Тесты используют временную папку, чтобы не создавать лишние JSON-файлы в рабочем каталоге.

Запуск всех тестов:
```bash
mvn test
```

---
## 🏗️ Архитектура и решения

- **Model:** ShortLink, User
- **Service:** бизнес-логика: сокращение ссылок, переходы, уведомления, автоочистка TTL
- **Util:** JSON-хранилище, генератор кода, конфигурация
- **Main:** консольное управление

#### Особенности реализации:
- **UUID** идентифицирует пользователей без авторизации.
- **Автоочистка** ссылок работает каждые 60 секунд через ScheduledExecutorService.
- **Уведомления** выводятся в консоль при почти истекшем TTL или при последнем переходе.
- **TTL и лимиты переходов** берутся из конфигурации или задаются пользователем.

---

